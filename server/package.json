{
  "name": "chat-node-express-sequelize",
  "version": "0.0.1",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "NODE_ENV=development && nodemon src/server",
    "start": "NODE_ENV=production && nodemon src/server"
  },
  "author": "Leif Lindbäck",
  "license": "BSD-3-Clause",
  "dependencies": {
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "sequelize": "^5.8.12",
    "mariadb": "^2.0.5",
    "body-parser": "^1.19.0",
    "verror": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^6.0.0",
    "eslint-config-google": "^0.13.0"
  }
}
