{
  "name": "chat-node-express-sequelize",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "export NODE_ENV=development && nodemon index",
    "start": "export NODE_ENV=production && nodemon index"
  },
  "author": "Leif Lindbäck",
  "license": "BSD-3-Clause",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^6.0.0",
    "eslint-config-google": "^0.13.0"
  }
}
